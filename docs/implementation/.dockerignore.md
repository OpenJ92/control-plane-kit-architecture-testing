Source: [.dockerignore](../../.dockerignore).
Maintain this document alongside its source file. When the source or relevant
imported contracts change, verify and update this companion in the same change.

These Docker context exclusions omit Git/CI metadata, caches, local environments and build output if a Docker build uses this repository as its context. The current test.sh uses read-only bind mounts and docker run; .dockerignore does not filter a bind mount. Do not treat these patterns as a secret scan or proof that mounted source excludes private local files. Review the actual build/mount caller before relying on this configuration.
